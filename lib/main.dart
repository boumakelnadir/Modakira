import 'package:flutter/material.dart';

void main() {
  runApp(const Modakira());
}

class Modakira extends StatelessWidget {
  const Modakira({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
